# Copy to `.env` and fill in real values. `.env` is git-ignored by default;
# `.env.example` is checked in as documentation of what's expected.
#
# Values already set in the real environment always win over `.env` — this
# file is a local-dev convenience, not a source of truth for production.

# DEBUG=true
# DATABASE_URL=sqlite:///./db.sqlite3
