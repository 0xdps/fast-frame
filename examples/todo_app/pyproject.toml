[project]
name = "todo_app"
version = "0.1.0"
description = "FastFrame project"
requires-python = ">=3.11"
dependencies = [
    "fastframe>=0.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
